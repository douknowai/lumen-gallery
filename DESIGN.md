# DESIGN.md

## 项目与用户画像
- LUMEN 流明 · 3D 虚拟美术馆，策展式沉浸体验；本轮新增「AI 语音讲解」，让每件展品以第一人称与观众对话。

## 品牌与视觉方向
- 延续既有 LUMEN 设计语言：纸底 + 黄铜 accent + 衬线字体，保持「美术馆纸墨感」。
- AI 语音讲解采用「打电话」隐喻：展品是通话对象，UI 表现为通话卡片 + 展品头像 + 左右对话气泡，而非技术面板。

## Design Tokens
### 色彩
- 纸底 `var(--paper)`；次级底 `var(--paper-dim)`；描边 `var(--line)`
- 主 accent `var(--brass)`；录音/警示态 `#a63d2f`（黄铜红）
- 正文 `var(--ink)`；次要文字 `var(--stone)`

### 字体
- 展品名/标题：`font-serif-lumen`
- 编号/状态/元信息：`font-mono-lumen`

## 组件规范
- 通话面板：桌面居中卡片（max-w-md，圆角 18px）；移动端底部弹层（78vh，顶部圆角 20px）。
- 气泡：assistant 靠左 + 纸灰底（左下 4px）；user 靠右 + 黄铜底白字（右下 4px）。
- 麦克风按钮：圆形 64px 黄铜底，录音中转为警示红并脉冲。

## 交互与状态
- 状态逐段提示：聆听 → 识别 → 思考 → 播报。
- 按住说话（Pointer Down/Up），松手结束；生成中禁止录音与「听介绍」。
- 弹层进出遵循 spring 260/30，与展品弹窗一致。

## 通话形态（语音讲解升级）
- 通话不再锁定于弹窗，可折叠成悬浮胶囊 + 底部字幕，观众边走动边对话、边看展品。
- 字幕条：AI 播报/回复以字幕显示在页面底部居中（影视字幕质感：纸底 + ink 文字 + 底部轻遮罩）；观众说的话转录为字幕，以更小一号文字置于其上，用「你」前缀或黄铜点区分。
- 输入模式：「按住说话」（空格快捷键 / 长按麦克风）与「免提（自动 VAD）」二选一，持久化记忆。
- 音色选择：在角色选择面板新增「讲解音色」，精选女声（小荷 / 灿灿）与男声（云舟 / 一尘），默认小荷。

## 设计禁忌
- 不使用科技蓝、蓝紫渐变等「万能 AI 风格」，坚持纸墨 + 黄铜的策展质感。
- 不刻意堆砌硬件电话图标，保持「与展品对话」的策展语境。